import { ReactNode } from 'react'
import { ImageProps } from 'next/image'

export default interface box
{
   className?: string,
   img:ImageProps
   title:string
   description:string
   overCotent?:ReactNode
};;;;;;;;;;
